package-json-path:
        description: "Path of the package.json"
        required: true
        type: string
      tag-prefix:
        description: "Prefix of the release tag"
        required: true
        type: string
        const pj = require('./src/PlatformDesignStudio.UI/package.json')

            console.log(pj.version)
        
